export interface IProject {
  header: string,
  description: string,
  link: string,
  stack: string[]
}

export interface IWork {
  period: string,
  title: string,
  tag: string,
  description: string,
  stack: string[]
}
